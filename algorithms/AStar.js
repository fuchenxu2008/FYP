class AStar {
    constructor(graph) {
        this.graph = graph;
    }

    calDistance(start, goal) {
        this.graph.getRoad(start);
        this.graph.getRoad(start);
    }

    run() {

    }
}

module.exports = AStar;
